version: 2.1

jobs:
  build:
    docker:
      - image: ruby:2.7.5
    resource_class: large
    steps:
      - checkout
      - run:
          name: Install the needed gems
          command: bundle install --without production
      - run:
          name: Run rails test
          command:  rails test
workflows:
  ruby_ci: 
    jobs:
      - build
